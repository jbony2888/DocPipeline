# Optimized requirements for Streamlit Cloud (CPU-only, smaller memory footprint)
streamlit
pydantic
PyMuPDF

# CPU-only PyTorch (much smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# EasyOCR with CPU-only dependencies
easyocr

# Optional: Google Cloud Vision (if you want that option)
# google-cloud-vision


