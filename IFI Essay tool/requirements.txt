# Core dependencies (always needed)
streamlit
pydantic
PyMuPDF

# CPU-only PyTorch for EasyOCR (much smaller for Streamlit Cloud)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# EasyOCR for local OCR (works on Streamlit Cloud with CPU-only torch)
easyocr

# Google Cloud Vision (optional - only if you add credentials)
google-cloud-vision

# Groq for LLM-based extraction (free, fast)
groq

# OpenAI for highest accuracy LLM extraction
openai

# Testing
pytest

# Supabase authentication
supabase
requests

