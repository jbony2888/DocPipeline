# Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to Git (it's in .gitignore)

# ========== Required for app + worker ==========
# Supabase
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# Redis (job queue)
# - Local dev: redis://localhost:6379/0 (run Redis locally: brew install redis && redis-server)
# - Docker: redis://redis:6379/0
# - Production (e.g. Render): use your Redis URL from the host (e.g. redis://red-xxx:6379)
# If Redis is unreachable (e.g. local app with production REDIS_URL), uploads process synchronously instead.
REDIS_URL=redis://localhost:6379/0

# Flask
FLASK_SECRET_KEY=generate-a-secure-random-key-here
FLASK_PORT=5000

# ========== Required for OCR (choose one) ==========
# Option A: Path to Google Cloud service account JSON (local / Docker mount)
GOOGLE_APPLICATION_CREDENTIALS=./credentials_vision.json
GOOGLE_CREDENTIALS_PATH=./credentials_vision.json
# Option B: Full JSON string (e.g. for Render / env-only). Use GOOGLE_CLOUD_VISION_CREDENTIALS_JSON=...
# GOOGLE_CLOUD_VISION_CREDENTIALS_JSON={"type":"service_account",...}

# ========== Recommended ==========
# Groq (LLM extraction). Without it, pipeline uses fallback extraction.
GROQ_API_KEY=your-groq-api-key-here

# ========== Optional ==========
# Worker ID (defaults to worker-<pid>)
WORKER_ID=worker-1

# Email (Gmail only): smtp.gmail.com port 587 (STARTTLS). EMAIL must be a Gmail address; GMAIL_PASSWORD = 16-char app password (spaces optional).
# EMAIL=your-gmail@gmail.com
# GMAIL_PASSWORD=xxxx xxxx xxxx xxxx
APP_URL=http://localhost:5000

# Batch script / tests
# OWNER_USER_ID=uuid-for-test-user
# TEST_OWNER_USER_ID=uuid-for-test-user

# OpenAI (optional alternative to Groq for LLM)
# OPENAI_API_KEY=

# Auth callback (if using separate auth service)
# FLASK_AUTH_URL=http://localhost:5001
# STREAMLIT_URL=http://localhost:8501
